#include <iostream>
#include <algorithm>
#include <fstream>
#include <vector>
using namespace std;
int main() {
    FILE* fp;
    int N, K;
    vector <int> requirement;
    freopen(&fp, "stat.in", "r", stdin);
    fscanf(fp, "%d", &N);
    for(int i = 0, i < N, i++){
        int buffer;
        fscanf(fp, "%d", &buffer);
        //
    }
    fscanf(fp, "%d", &M);
    for(int i = 0, i < M, i++){
        int buffer;
        fscanf(fp, "%d", &buffer);

    }
    freopen(&fp, "stat.out", "w", stdout);


    return 0;
}
